Options -Indexes

# Allow CORS preflight
RewriteEngine On
RewriteCond %{REQUEST_METHOD} OPTIONS
RewriteRule ^(.*)$ $1 [R=200,L]

# Route all requests through index if needed
# (Not required for direct PHP file access)
